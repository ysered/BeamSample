<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:maxHeight="?android:attr/listPreferredItemHeightLarge"
    android:minHeight="?android:attr/listPreferredItemHeight"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/doneImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/marginSmall"
        android:layout_marginLeft="@dimen/marginDefault"
        android:layout_marginStart="@dimen/marginDefault"
        android:layout_marginTop="@dimen/marginSmall"
        android:contentDescription="@null"
        android:src="@drawable/ic_done"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/summaryText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="@dimen/marginDefault"
        android:layout_marginLeft="@dimen/marginLarge"
        android:layout_marginRight="@dimen/marginDefault"
        android:layout_marginStart="@dimen/marginLarge"
        android:layout_marginTop="@dimen/marginSmall"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/doneImage"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Make coffee" />

</android.support.constraint.ConstraintLayout>
